[workspace]
members = [
    "apps/proxy",
    "apps/api",
    "apps/cli",
    "crates/model",
    "crates/repository",
    "crates/dynamodb",
    "crates/error",
]
resolver = "3"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
